/*package Array;

public class StartOfArray {
		
	// Empty array with size 5
	int[] arr = new int[5];

	// Array with elements
	int[] arr1 = {1, 2, 3, 4, 5};
	// Access the first element
	int firstElement = arr[0];

	// Access the third element
	int thirdElement = arr[2]
	
	// Modify the first element
	//arr[0] = 10;

	// Modify the third element
	arr[2] = 30;
	
	// Using a for loop
	for (int i = 0; i < arr.length; i++) {
	    System.out.println(arr[i]);
	}

	// Using a for-each loop
	for (int element : arr) {
	    System.out.println(element);
	}
	
	//You can find the length of an array using the length property.
	int length = arr.length;
	
	// Using System.arraycopy
	int[] newArr = new int[arr.length];
	System.arraycopy(arr, 0, newArr, 0, arr.length);

	// Using Arrays.copyOf
	int[] newArr = java.util.Arrays.copyOf(arr, arr.length);
	
	//Length of a String-You can find the length of a string using the length method.
	int length = str1.length();
	
	//concate
	String str3 = str1 + " World";
	String str4 = str1.concat(" World");
	
	//You can extract characters from a string using the charAt method.
	char ch = str1.charAt(1); // 'e'
	
	//You can extract substrings using the substring method.
	String substr1 = str1.substring(1);  // "ello"
	String substr2 = str1.substring(1, 4);  // "ell"
	
	//You can replace characters or substrings using the replace and replaceAll methods.
	String replacedStr1 = str1.replace('l', 'p');  // "Heppo"
	String replacedStr2 = str1.replace("ll", "pp");  // "Heppo"
	String replacedStr3 = str1.replaceAll("l+", "p");  // "Heppo
	
	//You can convert the case of a string using the toLowerCase and toUpperCase methods.
	String lowerCaseStr = str1.toLowerCase();  // "hello"
	String upperCaseStr = str1.toUpperCase();  // "HELLO"
	
	//You can split a string into an array of substrings using the split method
	String[] parts = str1.split(" ");
	
	//You can check if a string contains a specific substring using the contains method.
	boolean containsHello = str1.contains("Hell");  // true
	
	//You can search for substrings using the indexOf and lastIndexOf methods.
	int index1 = str1.indexOf('e');  // 1
	int index2 = str1.indexOf("ll");  // 2
	int lastIndex = str1.lastIndexOf('l');  // 3
}*/
