String class methods :

	1) int length()
		String str = "Hi";
		int len = str.length();
   
	2) char charAt(int index) 
		char ch = str.charAt(1); // 'i'
		
		String str = "technocredits";
		str.charAt(str.length()-2);

   
	3) String toLowerCase()
   
	4) String toUpperCase()
   
	5)To convert String to Int [ParseInt]
		(String.ParseInt(ch))
   
	6) To convert char to int [getNumericValue]
		(String.getNumericValue(ch))
	
	7) String valueOf(char ch)
		To convert char to string [valueof]
		(String.valueOf(ch))
	
	8) String[] split(String delimiter)
		str = "hd-yw726y-djhs-7gjdh"
		String[] arr = str.split(" ");
	 
	9) boolean contains(String str) [ to check the value of character ]
		str.contains(ch)
	
	10) int indexOf(char ch)
		index == str.indexOf(ch)
	
	11) int lastIndexOf(char ch)
		index == str.lastIndexOf(ch)
	
 	12) boolean contains(String str) 
	
	13) String replace(char ch1, char ch2)
		str = str.replace(currentCh, "");
	
	14) String replace(String str1, String str2)
	
	15) String replaceFirst(String str1, String str2)

   

Character class methods:
	1) boolean Character.isDigit(ch))
	
	2) boolean Character.isLetter(ch)
	
    3) boolean Character.isUpperCase(ch)
	
	4) boolean Character.isLowerCase(ch)
	
    5) char Character.toLowerCase(ch)
	
	6) char Character.toUpperCase(ch)
	
    7) boolean Character.isWhiteSpace(ch)	


